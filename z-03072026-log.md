# Conversation Log - 03 Jul 2026

## Context
Project: `Project_InvoiceProgressMonitor`

This file records the continuation after `z-02072026-log.md`.

## Conversation Summary

### 1. Read Previous Session Log
- User asked to read the last conversation and change log from `z-02072026-log.md`.
- The previous log showed that the app had been scaffolded as a React + Vite + Supabase project.
- The previous session also covered Supabase connectivity, admin login, blank-page fix, dashboard expansion, submit/approval concept for `nilai_progress`, and the `permission denied for table users` trigger issue.

### 2. Permission Issue Confirmed Clear
- User confirmed the permission issue was clear/resolved.
- Work moved to a new topic.

### 3. Monthly vs Termin Input Difference
- User asked why `monthly` and `termin` had different methods for inputting `nilai_progress`.
- Finding:
  - `monthly` used the `Input & Edit` workflow with submit/approval.
  - `termin` used the separate `Setup Termin` modal and `setup_termins()` RPC.
- User confirmed that monthly and termin are different contextually, but the input workflow should be standardized.

### 4. New Workflow Decision
- User decided:
  - `termin` should go through the same submit/approval workflow as `monthly`.
  - `Setup Termin` is no longer needed and should be removed.
  - Users should manually input both monthly and termin rows from the selected month filter.
  - Monthly and termin rows should be mixed together in the `Input & Edit` tab.
  - `pct_termin` should be editable.

## Implementation Completed

### 1. Removed Setup Termin Flow
Updated file:
- `src/components/InvoiceProgressMonitor_v2.jsx`

Changes:
- Removed `SetupTerminModal` UI flow.
- Removed `Setup Termin` tab from navigation.
- Removed modal-related state and handlers.
- Navigation now contains:
  - `Summary`
  - `Input & Edit`
  - `Generate Monthly`
  - `Kelola User` for admin only

Updated file:
- `src/hooks/useMonthlyStatus.js`

Changes:
- Removed `setupTermins()` hook function.
- Removed `setupTermins` from the hook return object.

Updated file:
- `schema_COMPLETE.sql`

Changes:
- Removed obsolete `setup_termins()` RPC function from the schema source.

### 2. Added Manual Monthly/Termin Creation
Updated file:
- `src/components/InvoiceProgressMonitor_v2.jsx`

Changes:
- Added manual create form inside `Input & Edit`.
- Users can create either:
  - `monthly` row
  - `termin` row
- New row creation uses the active loaded period filter.
- For `termin`, the user must input:
  - `no_termin`
  - `pct_termin`
- Optional `nilai_progress` can be entered at creation time and is submitted into the approval workflow.

Updated file:
- `src/hooks/useMonthlyStatus.js`

Changes:
- Added `createInvoice()` hook method.
- The method inserts into `monthly_status` directly.
- `invoice_seq` is calculated as the next sequence for the same project, period, and event type.
- If `nilai_progress` is entered during creation, it is marked as submitted but not approved.

### 3. Mixed Monthly and Termin Rows in Input & Edit
Updated file:
- `src/components/InvoiceProgressMonitor_v2.jsx`

Changes:
- `Input & Edit` now displays `monthly` and `termin` rows together in one table.
- Rows are sorted by project, event type, and invoice sequence.
- Each row shows an event type badge:
  - `monthly`
  - `termin`
- Termin rows show `no_termin` under the badge.

### 4. Standardized Nilai Progress Workflow
Updated file:
- `src/components/InvoiceProgressMonitor_v2.jsx`

Changes:
- Both monthly and termin rows use the same `nilai_progress` input.
- Both monthly and termin rows use the same submit/approval state display:
  - `Draft`
  - `Submitted`
  - `Approved`
- Editors can submit `nilai_progress` when not locked.
- Admin can approve or reject submitted `nilai_progress`.

Updated file:
- `src/hooks/useMonthlyStatus.js`

Changes:
- Existing `submitNilaiProgress()` and `approveNilaiProgress()` continue to work for both event types because they update by row `id`.

### 5. Added Editable pct_termin
Updated file:
- `src/components/InvoiceProgressMonitor_v2.jsx`

Changes:
- `pct_termin` is editable for `termin` rows in the `Input & Edit` table.
- Value is saved through the new `updateInvoiceFields()` hook method.

Updated file:
- `src/hooks/useMonthlyStatus.js`

Changes:
- Added `updateInvoiceFields()` to update metadata fields such as `pct_termin`.

### 6. Schema Source Updated
Updated file:
- `schema_COMPLETE.sql`

Changes:
- Added approval tracking columns to `monthly_status`:
  - `nilai_progress_submitted`
  - `nilai_progress_submitted_by`
  - `nilai_progress_submitted_at`
  - `nilai_progress_approved`
  - `nilai_progress_approved_by`
  - `nilai_progress_approved_at`
- Added insert RLS policy for editors/admins to create invoice rows in allowed departments.
- Updated `v_monthly_progress` definition to include nilai progress approval fields.
- Removed `setup_termins()` RPC from schema source.

## Technical Notes

### Data Fetch Change
`useMonthlyStatus()` now fetches from `monthly_status` with an inner join to `projects` instead of relying on `v_monthly_progress`.

Reason:
- The app needs fields such as `nilai_progress_submitted` and `nilai_progress_approved`.
- These fields were not present in the old view output during the current implementation state.
- The hook flattens project fields into the row object so the UI remains simple.

### Required Database Application
If the live Supabase database does not already have the added columns/policies, run the relevant SQL from the updated `schema_COMPLETE.sql` or a targeted migration before using the new workflow in production.

## Verification

Command run:

```bash
npm run build
```

Result:
- Build succeeded.
- Vite emitted only the existing chunk-size warning.

=========================

## Consolidated Change Export - Final 03 Jul 2026 State

### Scope Covered in This Session
- Summary tab restoration and enhancement
- Input & Edit workflow changes
- Manual row creation flow
- All payment type support for Input & Edit
- Supabase SQL migration export and migration-script fix

### Frontend Changes

#### 1. Summary Tab
Updated file:
- `src/components/InvoiceProgressMonitor_v2.jsx`

Final state:
- Restored the old Summary dashboard visual structure.
- KPI cards now show:
  - `Billing Events`
  - `Total Nilai Progress`
  - `Konversi Piutang Usaha`
  - `Overdue`
- `Konversi Piutang Usaha` calculation changed to:
  - count of rows with `piutang_usaha = Done`
  - divided by total billing events in the applied filter
  - displayed as percentage only
- Restored pipeline charts:
  - `Pipeline — Jumlah Event`
  - `Pipeline — Nilai Progress (juta Rp)`
- Added `Status per Departemen` chart in Summary.
- Added `Watchlist Overdue` card below the departemen chart.
- Empty state for overdue watchlist:
  - `Tidak ada proyek overdue`

#### 2. Input & Edit Tab
Updated files:
- `src/components/InvoiceProgressMonitor_v2.jsx`
- `src/hooks/useMonthlyStatus.js`

Final state:
- `Pct` column renamed to `Progress (%)`.
- `Nilai Progress` and `Progress (%)` are now entered in separate cells.
- One `Submit` action now sends both values together.
- Both fields lock when the row is already submitted or approved.
- `Progress (%)` input is visible for all row types.
- Manual row creation remains in the `Input & Edit` tab.

#### 3. Payment Type Support in Input & Edit
Updated files:
- `src/components/InvoiceProgressMonitor_v2.jsx`
- `src/hooks/useMonthlyStatus.js`

Final state:
- Added support for all payment types in `Input & Edit` create flow:
  - `monthly`
  - `termin`
  - `progress_payment`
  - `milestone`
  - `turnkey`
- Added friendly payment type labels and color-coded badges for all types.
- Replaced `no_termin` usage in the UI with `invoice_label`.
- `invoice_label` field is visible in the create form for all types.
- `Progress (%)` field is visible in the create form for all types.
- Hook create flow now writes `invoice_label` instead of `no_termin`.

### Backend / SQL Changes

#### 4. Schema Source Changes
Updated file:
- `schema_COMPLETE.sql`

Changes recorded in source:
- Added nilai progress submit/approval columns.
- Removed obsolete `setup_termins()` RPC from schema source.
- Added editor/admin insert policy for `monthly_status`.
- Extended the view definition to include approval fields.

#### 5. SQL Update Export
Created file:
- `sql-update.txt`

Purpose:
- Safe incremental SQL update for Supabase without replacing the full schema.

Contents include:
- add approval columns if missing
- backfill boolean defaults
- add insert policy
- remove obsolete `setup_termins()` RPC
- refresh `v_monthly_progress`

#### 6. SQL Migration Export
Created file:
- `SQL-Migration.txt`

Purpose:
- Migration for all payment type support and rename from `no_termin` to `invoice_label`

Contents include:
- rename `no_termin` to `invoice_label`
- extend `event_type` constraint to all payment types
- refresh label and pct constraints
- refresh `v_monthly_progress`

#### 7. SQL Migration Fix
Updated file:
- `SQL-Migration.txt`

Reason:
- Supabase returned error:
  - `cannot change name of view column "no_termin" to "invoice_label"`

Fix applied:
- Added safe pre-step:
  - `ALTER VIEW public.v_monthly_progress RENAME COLUMN no_termin TO invoice_label`
- Wrapped in conditional `DO $$ ... $$` logic so migration remains rerunnable.

### Verification Performed
Command run repeatedly after relevant changes:

```bash
npm run build
```

Results:
- Build succeeded after each completed implementation batch.
- Only the existing Vite chunk-size warning remained.

### Final Functional State
- `Setup Termin` flow removed from active app.
- Summary tab restored and enhanced.
- Input & Edit supports unified submit for `nilai_progress` and `progress (%)`.
- Input & Edit create flow supports all payment types.
- UI now uses `invoice_label` instead of `no_termin`.
- SQL migration files exported for Supabase application.

=========================

## Conversation Continuation - Input & Edit All Projects Listing

### User Request
- User clarified that Input & Edit should display all projects by default, not only projects with existing `monthly_status` rows.
- User requested:
  - show all projects as rows for the selected period
  - show `No Data - Click to create` for projects without period data
  - remove `Generate Monthly` from the UI entirely
  - keep pagination with 10 projects per page
  - sort by departemen order:
    - `INF1`
    - `INF2`
    - `GED`
    - `E&I`
    - `PKA`
  - default period should follow the current month

### Changes Applied
Updated files:
- `src/hooks/useMonthlyStatus.js`
- `src/components/InvoiceProgressMonitor_v2.jsx`

Changes:
- Reworked data-loading logic to combine:
  - all projects from `projects`
  - selected-period rows from `monthly_status`
- Projects without existing rows now appear with placeholder state.
- Added `Create` button in placeholder rows with message:
  - `No Data - Click to create`
- Added pagination in Input & Edit with 10 projects per page.
- Added departemen sort priority in the hook.
- Default period now uses the current month.
- Removed `Generate Monthly` tab and related UI references.

### Verification
Command run:

```bash
npm run build
```

Result:
- Build succeeded.
- Vite emitted only the existing chunk-size warning.

=========================

## Conversation Continuation - All Payment Types in Input & Edit

### User Request
- User requested support for all payment types in `Input & Edit` data only.
- User requested exporting the SQL migration to `SQL-Migration.txt`.
- User requested renaming `no_termin` to `invoice_label`.
- User requested `invoice_label` and `progress (%)` fields to stay visible for all types.

### Changes Applied
Updated files:
- `SQL-Migration.txt`
- `src/hooks/useMonthlyStatus.js`
- `src/components/InvoiceProgressMonitor_v2.jsx`

Changes:
- Exported Supabase migration file `SQL-Migration.txt`.
- Migration covers:
  - rename `no_termin` to `invoice_label`
  - extend `event_type` to support `monthly`, `termin`, `progress_payment`, `milestone`, `turnkey`
  - refresh constraints and view definition
- Updated Input & Edit create flow to use `invoice_label`.
- Added all payment types to the create form dropdown.
- Updated row type badges to show friendly labels and distinct colors for all payment types.
- Kept changes limited to Input & Edit data flow and related hook support.

### Verification
Command run:

```bash
npm run build
```

Result:
- Build succeeded.
- Vite emitted only the existing chunk-size warning.

=========================

## Conversation Continuation - Input & Edit Unified Submission

### User Request
- User requested changes in `Input & Edit` tab only:
  - rename `Pct` column to `Progress (%)`
  - allow users to input `nilai_progress` and `progress (%)` together
  - submit both fields in one step using the same `Submit` button
  - apply `Progress (%)` field to monthly and other billing types, not only termin

### Changes Applied
Updated files:
- `src/components/InvoiceProgressMonitor_v2.jsx`
- `src/hooks/useMonthlyStatus.js`

Changes:
- Renamed table column `Pct` to `Progress (%)`.
- `Progress (%)` input is now shown for all rows.
- Removed separate auto-save behavior for `pct_termin`.
- Unified submit flow so `Submit` now sends both `nilai_progress` and `progress (%)` together.
- Locked both fields when the row is already submitted or approved.

### Verification
Command run:

```bash
npm run build
```

Result:
- Build succeeded.
- Vite emitted only the existing chunk-size warning.

=========================

## Conversation Continuation - Summary KPI Revision

### User Request
- User requested revising the Summary KPI card `Nilai AR Terbentuk`.
- New label: `Konversi Piutang Usaha`.
- New formula: count of `piutang_usaha = Done` divided by total billing events in the applied filter.
- Display format: percentage only.
- Subtitle: `dari total events`.

### Changes Applied
Updated file:
- `src/components/InvoiceProgressMonitor_v2.jsx`

Changes:
- Replaced `nilaiAR` KPI calculation with `konversiPiutang` percentage calculation.
- Updated KPI card label to `Konversi Piutang Usaha`.
- Updated KPI card value format to percentage.
- Updated subtitle to `dari total events`.

### Verification
Command run:

```bash
npm run build
```

Result:
- Build succeeded.
- Vite emitted only the existing chunk-size warning.

=========================

## Conversation Continuation - Summary Tab Departemen Status and Watchlist

### User Request
- User aborted the previous departemen chart direction.
- User requested changes to `Summary` tab only:
  - add `Status per Departemen` chart containing count of status `opname`, `BAPP`, `submit invoice`, `correct invoice`, and `piutang usaha`
  - add `Watchlist Overdue` card below the departemen status chart
- User explicitly requested no changes to any other tab.

### Changes Applied
Updated file:
- `src/components/InvoiceProgressMonitor_v2.jsx`

Changes:
- Added `statusByDept` derived dataset for Summary only.
- Added `Status per Departemen` stacked bar chart showing count of `Done` values by stage for each departemen.
- Added `Watchlist Overdue` card below the departemen chart.
- Empty state message for watchlist: `Tidak ada proyek overdue`.
- No changes were made to `Input & Edit`, `Generate Monthly`, or `Kelola User` tabs.

### Verification
Command run:

```bash
npm run build
```

Result:
- Build succeeded.
- Vite emitted only the existing chunk-size warning.

## Final Status
- `Setup Termin` flow removed from active app.
- Monthly and termin rows are now managed from `Input & Edit`.
- `nilai_progress` workflow standardized for both monthly and termin.
- `pct_termin` editable for termin rows.
- Manual row creation added for monthly and termin.
- Build verification passed.

=========================

## Conversation Continuation - Summary Tab Design Restoration

### User Request
- User clarified that only the `Summary` tab design should be restored to the pre-refactor dashboard style.
- User explicitly requested no additional changes outside the Summary tab.

### Changes Applied
Updated file:
- `src/components/InvoiceProgressMonitor_v2.jsx`

Changes:
- Restored old Summary KPI layout:
  - `Billing Events`
  - `Total Nilai Progress`
  - `Nilai AR Terbentuk`
  - `Overdue`
- Restored the old two-chart Summary layout:
  - `Pipeline — Jumlah Event`
  - `Pipeline — Nilai Progress (juta Rp)`
- Calculations now use current Supabase-backed `rows` and `nilai_progress` field.
- Other tabs and workflows were left unchanged.

### Verification
Command run:

```bash
npm run build
```

Result:
- Build succeeded.
- Vite emitted only the existing chunk-size warning.

=========================

## Conversation Continuation - Modal Popup for Create Row in Input & Edit

### User Request
- User reported that clicking the "+ Create" button did nothing visible.
- Root cause clarification: the inline form fields were always visible on the page. User expected a popup/modal to appear when clicking "+ Create".
- User requested:
  - "+ Create" button opens a modal popup (not inline form).
  - Modal width: medium (480px), not full screen.
  - Modal contains form fields: Pilih Proyek, Event Type, Invoice Label, Progress (%), Nilai Progress.
  - Show success message inside the modal after creation (not a toast).
  - "Cancel" button closes the modal without submitting.
  - After successful creation: show success message, user clicks Cancel to dismiss.
  - Form resets on modal close.
  - No keyboard support (Escape and Enter should not trigger modal close or submission).

### Changes Applied
Updated file:
- `src/components/InvoiceProgressMonitor_v2.jsx`

Changes:
- Added three new state variables:
  - `isCreateModalOpen` - controls modal visibility.
  - `createModalSuccess` - shows success message inside modal after creation.
  - `createModalError` - shows error message inside modal if validation fails.
- Added `openCreateModal()` handler - resets form and opens modal.
- Added `closeCreateModal()` handler - closes modal and resets form and message states.
- Rewrote `createManualRow()` - validation errors and success messages now update modal state instead of firing global toast.
- Updated `handleCreateForProject()` - clicking "Create" on a "No Data" row now opens the modal with the project pre-selected.
- Replaced the always-visible inline form (5 input fields) with a clean header section showing only a description and the "+ Create" button.
- Added modal overlay with:
  - Semi-transparent dark backdrop.
  - 480px centered modal card.
  - Title: "Tambah Row Manual" with subtitle.
  - Close (X) button in top-right corner.
  - Labeled form fields: Pilih Proyek, Event Type, Invoice Label (with required indicator for non-monthly), Progress (%), Nilai Progress.
  - Error message area (rose) shown below fields on validation failure.
  - Success message area (emerald) shown below fields after successful creation.
  - Footer with "Cancel" button (always visible) and "Create" button (hidden after success).
- No changes made to any other tab (Summary, Kelola User).
- No changes made to `useMonthlyStatus.js`.

### Verification
Command run:

```bash
npm run build
```

Result:
- Build succeeded.
- Vite emitted only the existing chunk-size warning.
=========================

## Conversation Continuation - Lock Event Type Based on Modal Open Source

### User Request
- User asked if monthly payment type should also implement the modal form function.
- Clarification: all payment types already use the modal.
- User selected Option A: Lock event type to project's payment_type when opening from "No Data" table row.
- Keep event type editable when opening from the header "+ Create" button.

### Changes Applied
Updated file:
- `src/components/InvoiceProgressMonitor_v2.jsx`

Changes:
- Added new field to `newRow` state:
  - `event_type_locked` - boolean flag. Default: false.
- Updated `openCreateModal()` handler:
  - Sets `event_type_locked: false` when header "+ Create" button is clicked.
  - Event Type dropdown remains fully editable (free choice).
- Updated `handleCreateForProject()` handler:
  - Sets `event_type_locked: true` when "Create" button in "No Data" table row is clicked.
  - Pre-fills `event_type` with the project's `payment_type`.
- Updated `closeCreateModal()` handler:
  - Resets `event_type_locked: false` along with other form fields.
- Updated modal Event Type field UI:
  - When `event_type_locked === true`:
    - Shows a read-only colored badge with the locked event type.
    - Displays helper text: "Tipe dikunci sesuai proyek" (Type locked according to project).
    - User cannot change the event type.
  - When `event_type_locked === false`:
    - Shows normal dropdown (current behavior).
    - User can choose any event type.
- No changes made to `useMonthlyStatus.js`, Summary tab, or Kelola User tab.

### Verification
Command run:

```bash
npm run build
```

Result:
- Build succeeded.
- Vite emitted only the existing chunk-size warning.

### Functional Behavior
- Header "+ Create" button → modal opens with editable event type dropdown.
- "No Data - Click to create" button on monthly project row → modal opens with event type locked to "Monthly" badge.
- "No Data - Click to create" button on termin project row → modal opens with event type locked to "Termin" badge.
- Same for all other payment types.
=========================

## Conversation Continuation - Always Lock Event Type to Project Payment Type

### User Request
- User clarified: payment_type should **always** be locked to the value in `public.projects` table.
- User should **never be able to change** event type in the modal.
- Event type should always be derived from the selected project's `payment_type`.

### Changes Applied
Updated file:
- `src/components/InvoiceProgressMonitor_v2.jsx`

Changes:
- Updated `openCreateModal()` handler:
  - Now starts with `event_type: ""` (empty) instead of `"monthly"`.
  - Starts with `event_type_locked: false` (unlock state).
  - This indicates no project is selected yet.
- Updated modal "Pilih Proyek" dropdown `onChange` handler:
  - When user selects a project, the handler now:
    - Looks up the selected project's `payment_type` from the already-loaded `selectableProjects` list.
    - Auto-fills `event_type` with the found `payment_type`.
    - Sets `event_type_locked: true`.
    - No extra API call needed — data is already in memory.
  - When user clears the project selection (chooses empty option), `event_type` resets to `""` and `event_type_locked` resets to `false`.
- Updated modal Event Type field UI:
  - When `event_type_locked === true` and `event_type` has a value:
    - Shows colored badge (e.g., "Monthly", "Termin") with helper text "Tipe dikunci sesuai proyek".
    - User cannot edit this field.
  - When `event_type_locked === false` or `event_type` is empty:
    - Shows read-only placeholder text: "Pilih proyek dahulu" (Pick a project first).
    - No dropdown shown anywhere — event type is never user-editable.
- Removed the free Event Type dropdown entirely — it is no longer needed.
- No changes made to `handleCreateForProject()` — already locks correctly for "No Data" table rows.

### Reactive Workflow
**User flow when clicking header "+ Create":**
1. Modal opens → "Pilih Proyek" focused, Event Type shows "Pilih proyek dahulu"
2. User selects a project (e.g., "P001 — Project A")
3. Event Type field immediately updates to show the project's `payment_type` badge (e.g., "Termin")
4. Event Type is now locked, user cannot change it
5. User fills remaining fields (Invoice Label if needed, Progress %, Nilai Progress)
6. User clicks "Create"

### Verification
Command run:

```bash
npm run build
```

Result:
- Build succeeded.
- Vite emitted only the existing chunk-size warning.

### Summary
- Event Type is **always** sourced from `public.projects.payment_type`.
- Event Type is **never** user-editable.
- Event Type auto-fills **reactively** when a project is selected.
- No free dropdown exists anywhere in the modal.
=========================

## Conversation Continuation - Modal-Based Editing for Input & Edit Tab

### User Request - Interpretation A
- Inline editing for Nilai Progress and Progress (%) should be completely removed from Input & Edit table.
- Users should access a modal to edit/submit these values.
- Replace inline ""Submit"" button with modal-based workflow.
- Keep existing consistency: two separate modals (CREATE + EDIT).
- Supabase revision: NOT required (existing schema supports all operations).

### User Requirements Confirmed
1. **Create modal validation:** All payment types require:
   - Invoice Label (required for all, not just non-monthly)
   - Nilai Progress (required > 0)
   - Progress (%) (required > 0, not >= 0)
2. **Edit modal behavior:** User manually closes modal (no auto-close)
3. **Edit button visibility:** Disabled when admin is reviewing (nilai_progress_submitted but not approved)
4. **Empty value display:** Show ""-"" when nilai_progress or pct_termin is empty
5. **Cumulative display:** Remains visible in Progress (%) column
6. **Modal success message:** ""Nilai progress submitted untuk approval.""

### Changes Applied
Updated file:
- `src/components/InvoiceProgressMonitor_v2.jsx`

Changes made:

#### State Management
- Removed: `draftNilai` state (tracked inline editing of nilai_progress)
- Removed: `draftPct` state (tracked inline editing of pct_termin)
- Added: `editModalOpen` — controls edit modal visibility
- Added: `editingRow` — stores the row being edited
- Added: `editFormData` — holds pct_termin and nilai_progress for edit form
- Added: `editModalError` — displays validation errors in edit modal
- Added: `editModalSuccess` — displays success message in edit modal

#### Handlers
- Removed: `submitNilai()` — inline editing submission logic
- Added: `openEditModal(row)` — opens edit modal for existing row with current values pre-filled
- Added: `closeEditModal()` — closes edit modal and resets all states
- Added: `submitEditModal()` — validates and submits edited values to Supabase
  - Validates nilai_progress > 0
  - Validates pct_termin between 1-100
  - Calls existing `submitNilaiProgress()` hook
  - Shows success message (user must manually close)

#### UI Changes
- **Create Modal (existing):**
  - Invoice Label: now always shows required indicator (`<span className="text-rose-500">*</span>`)
  - Invoice Label placeholder: changed to ""Wajib diisi"" (was conditional)
  - Progress (%): now shows required indicator
  - Progress (%) placeholder: changed to ""Wajib diisi (1–100)""
  - Nilai Progress: now shows required indicator
  - Nilai Progress placeholder: changed to ""Wajib diisi""
- **Edit Modal (new):**
  - Opens when user clicks ""Edit"" button in Nilai Progress column
  - Shows project info and current values pre-filled
  - Same validation as create modal (nilai_progress > 0, pct_termin 1-100)
  - Shows error/success messages
  - User manually clicks ""Cancel"" or X to close
- **Input & Edit Table:**
  - Nilai Progress column: removed input field, now displays read-only text (shows ""-"" if empty)
  - Progress (%) column: removed input field, now displays read-only text (shows ""-"" if empty)
  - ""Submit"" button replaced with ""Edit"" button in Nilai Progress column
  - ""Edit"" button disabled when admin reviewing (`lockedNilai === true`)
  - ""Approve"" and ""Reject"" buttons remain unchanged for admin approval workflow
  - Cumulative Progress (%) still visible

#### Validation Logic
**Create Modal (all payment types):**
- Project: required
- Event Type: required (auto-filled from project.payment_type)
- Invoice Label: required for ALL types
- Nilai Progress: required > 0
- Progress (%): required > 0 and <= 100

**Edit Modal:**
- Nilai Progress: required > 0
- Progress (%): required > 0 and <= 100
(Project, Event Type, Invoice Label are read-only in edit modal)

#### Error Messages (Indonesian only)
- ""Pilih proyek terlebih dahulu""
- ""Pilih tipe event terlebih dahulu""
- ""Invoice label wajib diisi""
- ""Nilai progress wajib diisi dan lebih dari 0""
- ""Progress (%) wajib diisi antara 1-100""

### Verification
Command run:

```bash
npm run build
```

Result:
- Build succeeded.
- Vite emitted only the existing chunk-size warning (no new errors).

### Workflows After Implementation

**CREATE WORKFLOW (unchanged):**
1. User clicks header ""+ Create"" button
2. Modal opens with blank form
3. User selects project → event type auto-fills and locks
4. User fills: Invoice Label, Progress (%), Nilai Progress (all required)
5. User clicks ""Create"" button
6. Row created in table with all data

**EDIT WORKFLOW (new):**
1. User sees existing row in Input & Edit table with Nilai Progress and Progress (%) as read-only text
2. User clicks ""Edit"" button in Nilai Progress column
3. Edit modal opens with current values pre-filled
4. User edits Progress (%) and/or Nilai Progress
5. User clicks ""Submit"" button
6. Modal validates: nilai_progress > 0 and pct_termin 1-100
7. If valid: Values submitted to Supabase, status becomes ""Submitted"", success message shown
8. If invalid: Error message shown in modal, user can fix and resubmit
9. User manually clicks ""Cancel"" or X button to close modal
10. Modal closes, table refreshes with new values

**APPROVAL WORKFLOW (unchanged):**
1. Admin sees ""Submitted"" status and ""Approve""/""Reject"" buttons
2. Admin clicks ""Approve"" → status becomes ""Approved"", row locked
3. Or Admin clicks ""Reject"" → status reverts to ""Draft"", editor can edit again

### No Supabase Changes Needed
✅ All required columns already exist in `monthly_status` table
✅ Existing hook `submitNilaiProgress()` supports both create and edit workflows
✅ Zero database schema changes required
✅ RLS policies already cover edit operations

### Final State
- Input & Edit table has clean, read-only display for Nilai Progress and Progress (%)
- Both modal workflows (CREATE + EDIT) coexist independently
- All payment types require same mandatory fields
- Admin approval workflow unchanged
- Build verified with no errors

=========================

## Conversation Continuation - Unified Modal Workflow and Schema Alignment

### User Request
- User requested all phases to be executed
- User requested the inline Edit button to be removed from the table
- User requested the schema source and migration files to be aligned with the live Supabase state
- User requested step-by-step action reporting

### Changes Applied
Updated files:
- `sql-trigger-fix.txt`
- `schema_COMPLETE.sql`
- `src/components/InvoiceProgressMonitor_v2.jsx`

#### 1. SQL Trigger Migration
Created file:
- `sql-trigger-fix.txt`

Changes:
- Added `DROP TRIGGER IF EXISTS` guard for safe reruns
- Updated `compute_pct_termin_cumul()` to apply to:
  - `termin`
  - `progress_payment`
  - `milestone`
  - `turnkey`
- Kept cumulative progress validation at `> 100%`

#### 2. Schema Source Update
Updated file:
- `schema_COMPLETE.sql`

Changes:
- Updated `compute_pct_termin_cumul()` logic in the source schema
- Removed obsolete `generate_period()` RPC from the schema source

#### 3. Frontend Cleanup
Updated file:
- `src/components/InvoiceProgressMonitor_v2.jsx`

Changes:
- Removed the inline `Edit` button from the `Nilai Progress` column
- Kept approval buttons for admin users only
- Added/kept clarifying comments so the UI intent is explicit for all payment types
- Reworked placeholder `No Data - Click to create` rows in the Input & Edit tab to use the same `...` action menu pattern with `Create`
- Kept this change scoped to the Input & Edit tab only
- Restored the simple `Create` button on placeholder rows for all payment types
- Kept the Action column for existing rows as `Delete` only
- Removed the temporary `...` menu state and helper from the component

### Additional Input & Edit Adjustment
- Added a `Create` button in the same row for existing data rows when `nilai_progress` is empty
- The button appears in the `Nilai Progress` column
- The button opens the Create Modal with the current project pre-selected
- Applies to all payment types
- No changes were made to other tabs

### Row-Level Create Button Styling Update
- Updated the row-level `Create` button to use the Termin badge palette
- New styling uses violet colors (`bg-violet-50`, `text-violet-700`)
- This change applies only to the row-level `Create` button in `Input & Edit`

### Row-Level Empty State Design Alignment
- Changed the row-level empty `nilai_progress` state to match the placeholder `No Data - Click to create` design exactly
- The row now shows the same dashed container, left text, and right-side `Create` button
- The button keeps the original indigo styling used by the placeholder rows
- This applies only to the `Input & Edit` tab

### Stage Pill Restriction for Empty Nilai Progress
- Disabled stage pill toggling when `nilai_progress` is empty or `0`
- The restriction applies to the `Opname`, `BAPP`, `Submit Invoice`, `Correct Invoice`, and `Piutang Usaha` pills
- The pills now require `nilai_progress > 0` before they can be clicked
- This applies only to the `Input & Edit` tab

### Stage Pill Visibility Workflow
- Stage pills are now hidden entirely until `nilai_progress` is present
- The intended flow is: create row -> input `nilai_progress` -> stage pills appear -> user can toggle stages
- This applies only to the `Input & Edit` tab

### Full Row Visibility Control
- `Target Cash In` is now hidden until `nilai_progress` is present
- The `Delete` button is now hidden until `nilai_progress` is present
- This keeps the empty-row state minimal until the user has entered the correct data through the modal
- Applied only to the `Input & Edit` tab

### Unified Dashed Container for Nilai Progress
- The `Nilai Progress` column now uses the same dashed container design for both empty and filled rows
- Empty rows show `No Data - Click to create` on the left and `Create` on the right
- Filled rows show the numeric `nilai_progress` value and `StatusBadge` on the left, with `Create` on the right
- Approval buttons remain below the container when applicable
- Applied only to the `Input & Edit` tab

### Standardized Dashed Container Width Across Payment Types
- The dashed container now uses `flex-1` for the content area and `flex-shrink-0` for the `Create` button
- This keeps the monthly `No Data - Click to create` bar width aligned with all other payment types
- Added `gap-3` so spacing remains consistent across rows
- Applied only to the `Input & Edit` tab

### Empty Existing Row Colspan Alignment
- Existing rows with empty `nilai_progress` now use `colSpan={6}` like placeholder rows
- This makes the wide `No Data - Click to create` bar match the placeholder row width exactly
- Filled rows keep the normal single-column layout
- Applied only to the `Input & Edit` tab

### Step-by-Step Action Summary
1. Create the trigger-fix SQL migration file
2. Update the schema source to match the current live database design
3. Remove the inline Edit action so editing is no longer table-driven
4. Record the changes in this log file
5. Review the generated files before any Supabase SQL is executed

### Notes
- No table drop operations were added
- No data-destructive actions were introduced
- The live Supabase database should only receive the standalone `sql-trigger-fix.txt` migration after review

=========================

## Conversation Continuation - Hide Progress (%) Column When Empty

### User Request
- User identified that the `-` character in the Progress (%) column should be hidden when `nilai_progress` is empty
- The `-` was showing because the column was always rendered, even when the stage columns were hidden
- User wanted the Progress (%) column to also be hidden when `nilai_progress` is empty

### Changes Applied
Updated file:
- `src/components/InvoiceProgressMonitor_v2.jsx`

Changes:
- Modified line 661 to wrap the Progress (%) `<td>` in a conditional: `{row.nilai_progress && <td>...</td>}`
- Now the Progress (%) column is only rendered when `row.nilai_progress` exists
- This keeps the column hidden along with stage columns when `nilai_progress` is empty
- Filled rows continue to show Progress (%) normally

### Verification
Command run:

```bash
npm run build
```

Result:
- Build succeeded.
- Vite emitted only the existing chunk-size warning.

=========================

## Conversation Continuation - Search Bar for Input & Edit

### User Request
- User requested a search bar in the Input & Edit tab to search by:
  - `no_project`
  - `profit_center`
  - `nama_project`
  - `departemen`
- Search bar should be placed in the same horizontal row as Periode, Departemen, and Muat Data
- Layout: `Periode | Departemen | Search Bar | Muat Data`
- Partial matching, case-insensitive, OR logic across all 4 fields
- Search should persist (not reset) when "Muat Data" is clicked

### Changes Applied
Updated file:
- `src/components/InvoiceProgressMonitor_v2.jsx`

Changes:
- Added `searchQuery` state variable (line ~136)
- Added `appliedSearch` state variable to store the active search term only after `Muat Data` is clicked
- Added search input between Departemen select and Muat Data button (line ~369-370)
  - Placeholder: `"Cari proyek, profit center, departemen..."`
  - Width: `w-56` (matches Departemen select)
- Updated `onLoad()` so `Muat Data` is the only trigger that applies the search term
- Added `filteredRows` useMemo that filters `rows` by the applied search term across all 4 fields
- Updated `totalPages` and `paginatedRows` to use `filteredRows` instead of `rows`
- Updated page count display to show `filteredRows.length`
- Typing in the search input no longer filters immediately; filtering happens only after clicking "Muat Data"

### Verification
Command run:

```bash
npm run build
```

Result:
- Build succeeded.
- Vite emitted only the existing chunk-size warning.
