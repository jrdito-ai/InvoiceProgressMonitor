# Conversation Log - 04 Jul 2026

====== ISSUE FIXED xxxxx ======

## Context
Project: `Project_InvoiceProgressMonitor`

This file records the continuation after `z-03072026-log.md`.

## Conversation Summary

### 1. Incomplete Previous Month Visibility
- User requested that `Input & Edit` also show previous month billing events if they are not yet completed through `piutang_usaha`.
- Previous month rows are shown only when at least one stage is not `Done`.
- Users can continue updating stage pills on these rows.

### 2. Periode Column Added
- User requested a new `Periode` column.
- The column is displayed in `MMM-YYYY` format for all rows.

### 3. Previous Month Section Header
- User requested the previous month group header in Indonesian.
- Implemented header text:
  - `Progress Invoicing Belum Selesai dari MMMM-YYYY`

### 4. Section Header Removal
- User later decided the section headers were not necessary.
- Removed both the current month header and the previous month header.
- Also removed the background-color separation between current and previous month rows.

### 5. Pagination Merge
- User requested that previous month rows no longer use a separate pagination control.
- Merged current and previous month rows into a single paginated table stream.
- The page footer now reports combined totals while still noting previous month rows when present.

### 6. Runtime Fix
- After merging pagination, a stale `currentMonthFilteredRows` dependency remained in the component.
- Replaced the broken dependency with `filteredRows` so the page no longer crashes on load.

### 7. Periode Context Label
- User requested the Periode column to show whether a row is from the current month or previous month.
- Updated the Periode text to display `MMM-YYYY • Current` or `MMM-YYYY • Previous`.

### 8. Search Normalization
- User reported that searching by `no_project` or `profit_center` only returned previous-month rows.
- Normalized the search filter to also check canonical project master fields so current and previous month rows match consistently.

### 9. Sequential Stage Pills
- User requested stage pills to follow the correct order: Opname → BAPP → Submit Invoice → Correct Invoice → Piutang Usaha.
- Added sequential validation so a stage cannot be marked done unless all prior stages are already done.
- Clicking a disabled stage now shows a reason message, and toggling a completed stage back to not done cascades later stages back to not done.

### 10. Create Restriction
- User requested that a new row cannot be created for projects that still have incomplete rows from the previous month.
- Users are only allowed to update the stage pills for those previous-month incomplete rows.

### 11. Visual Styling
- Previous month rows use a gray-toned read-only style for `nilai_progress` and `pct_termin`.
- Previous month rows also show a visual indicator that they come from the previous month.

## Implementation Completed

### 1. Hook Updated
Updated file:
- `src/hooks/useMonthlyStatus.js`

Changes:
- Added previous month lookup using the selected period minus one month.
- Fetched current month and previous month rows separately.
- Filtered previous month rows to keep only incomplete ones.
- Added `month_group` flags so the UI can distinguish current vs previous month rows.

### 2. Input & Edit Updated
Updated file:
- `src/components/InvoiceProgressMonitor_v2.jsx`

Changes:
- Added `Periode` column to the table.
- Previous month rows are merged into the same pagination stream as current month rows.
- Added gray styling for previous month `nilai_progress` and `pct_termin`.
- Added a create-blocking validation when a project still has incomplete previous month rows.
- Kept stage pills editable on previous month rows.
- Removed the section headers and row background distinction after user feedback.
- Fixed a stale memo dependency that caused a blank page/runtime crash after pagination merge.
- Added a context label to the Periode column showing Current/Previous.
- Normalized search against project master fields to prevent current rows from being missed when searching by `no_project` or `profit_center`.
- Enforced sequential stage updates with validation and cascade reset of later stages when toggling backward.

### 3. Verification
Command run:

```bash
npm run build
```

Result:
- Build succeeded.
- Vite emitted only the existing chunk-size warning.

======== 2026-07-04T18:59:02+07:00Z ======

## Changes

### 1. Target Date Save Trigger Updated
- Updated `target_konversi_pu` and `target_cash_in` in `Input & Edit` so they no longer save on every input change.
- Added local draft state for both date fields so the selected date stays visible while the user is still interacting with the picker.
- Save now runs on `blur`, which means the selected date is only written after the user finishes selecting the date and leaves the field.
- Added a guard so unchanged values do not trigger unnecessary database updates.

### 2. Keterangan Lock After Save
- Updated `Simpan Keterangan` behavior so a successfully saved keterangan becomes non-editable immediately after the database refresh.
- The text input is now shown in a grayed-out disabled state when a persisted keterangan already exists.
- The `Simpan Keterangan` button is hidden completely once the keterangan has been stored.
- Previous-month rows remain read-only as before.

### 3. Updated File
- `src/components/InvoiceProgressMonitor_v2.jsx`

### 4. Verification
- Command run: `npm run build`
- Build succeeded.
- Vite emitted only the existing chunk-size warning.
